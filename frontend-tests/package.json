{
  "name": "frontend-tests",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run --reporter mochawesome --headless --browser chrome",
    "test:vrt": "percy exec cypress run --browser chrome --headless --spec cypress/integration/testsuite.spec.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@percy/cli": "^1.0.0-beta.48",
    "@percy/cypress": "^3.1.0",
    "cypress": "^7.1.0",
    "faker": "^5.5.3"
  },
  "dependencies": {
    "mocha": "^5.2.0",
    "mochawesome": "^6.2.2",
    "mochawesome-merge": "^4.2.0"
  },
  "description": ""
}
